# Archived PHP version

This is the original PHP implementation of the SimpleWPlugins site (plain PHP + Parsedown for the blog, meant to be deployed on a VPS via `git pull`). It has been superseded by the Hugo version at the repo root, deployed as a static site on GitHub Pages.

Kept here for reference — not deployed, not maintained going forward.
